{
  "name": "jest-to-log",
  "description": "Test log messages in jest",
  "version": "1.0.0",
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "scripts": {
    "test": "jest --watchAll",
    "check:pub": "npm pack && tar -xvzf *.tgz && rm -rf package *.tgz"
  },
  "files": ["/src", "CHANGELOG.md"],
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0",
    "scope-logger": "^1.2.0"
  }
}
